{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            // "command": "g++ -g -pthread src/main.cpp -o .bin/main.out",
            "command": "cd src && bazel build :main",
            "problemMatcher": ["$gcc"],
            "group": {
                "isDefault": true,
                "kind": "build"
            }
        }
    ]
}