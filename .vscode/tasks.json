{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "g++ -g -pthread src/main.cpp -o .bin/main.out",
            "problemMatcher": ["$gcc"],
            "group": {
                "isDefault": true,
                "kind": "build"
            }
        }
    ]
}